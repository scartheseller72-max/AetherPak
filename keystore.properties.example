# Copy to keystore.properties (gitignored) and fill in real values to sign release builds.
# When this file is absent, the release build falls back to debug signing.
storeFile=/absolute/path/to/release.keystore
storePassword=CHANGE_ME
keyAlias=aetherpak
keyPassword=CHANGE_ME
